# MFMS Phase 2 — Data Integrity & Firestore Cost Sprints

**Marble Factory Management System (MFMS)**
**Document version:** 1.0
**Date:** August 2026
**Status:** Planning document (post–Phase 1 / parallel to v1.1)
**Sprints:** S36–S41

---

## 1. Purpose

This document plans the remediation of **four defects** found in the Firestore data
layer audit. Two are silent data-corruption bugs; two are the cause of the
**daily free-tier quota being exceeded**.

Related documents:

- [`MFMS_v1.1_Roadmap.md`](./MFMS_v1.1_Roadmap.md) — feature backlog (S25–S30)
- [`MFMS_Phase1_Identity_Onboarding_Sprints.md`](./MFMS_Phase1_Identity_Onboarding_Sprints.md) — identity (S31–S35)
- **This file:** data integrity + read/write cost (S36–S41)

This phase adds **no new features**. Every sprint either prevents data corruption
or reduces document reads/writes.

---

## 2. Baseline defects

| # | Defect | Type | Quota impact |
|---|--------|------|--------------|
| **D1** | Stock read-modify-write without atomicity (`rawMaterials`, `finishedGoods`, `productionBatches`) | Correctness | None |
| **D2** | Sequence numbers generated by counting whole collection (16 sites) | Correctness **+ cost** | High (write path) |
| **D3** | Dashboard: 17 unbounded `snapshots()` listeners, no date window, re-attached per entry | Cost | **Critical** |
| **D4** | `syncCustomerBalance` downloads all factory sales orders + loads to sync one customer; write amplification per payment | Cost | High |

### D1 detail

`raw_material_repository.dart:135-194` reads `currentStock`, computes
`newStock` in Dart, writes the absolute value in a `WriteBatch`. The batch makes
the writes atomic but the read is outside it — concurrent movements overwrite
each other. `stockTransactions` rows are still written, so the ledger and the
stock level permanently disagree. `averageCost` corrupts the same way, which
propagates into `stockValue` → material cost → `netProfit`.

### D2 detail

16 functions use `.get()` on the collection and `docs.length + 1`. Three failure
modes: (a) concurrent creates produce duplicate numbers; (b) **deleting** a
document makes the next create *reuse* a live number — no concurrency needed, and
`customer_repository` cascade-deletes across 11 collections; (c) O(n) reads per
create, growing forever. Creating one job-work order triggers 4 full-collection
scans (order + load + invoice + collection numbers).

### D3 detail

`dashboard_bloc.dart:191-350` attaches 17 listeners, all
`where('factoryId', ...)` with no `limit()` and no date filter.
`_onWatchStarted` calls `_cancelSubscriptions()` then re-attaches all 17, so every
dashboard entry re-reads all history. Only 9 `limit()` calls exist in the whole
codebase.

**Key finding:** `DashboardFinancePeriod` (daily…allTime) already exists, but the
range is applied **client-side** via
`dashboard_cashflow_metrics.dart:68 contains(date, start, end)` — after download.
The window never reaches the query. This makes S39 a smaller change than expected.

### D4 detail

`customer_ledger_service.dart:54-76` calls
`watchSalesOrders(factoryId).first` and `watchLoads(factoryId).first`. Two
problems: it is factory-scoped for a single-customer calculation, and `.first`
on a `snapshots()` stream opens a listener, takes one event, discards it — no
cache reuse, full billed read every call.

Write amplification per job-work payment: 4 writes in the transaction, then
`_syncInvoiceFromPayments` rewrites invoice+load+order (3), then
`refreshContainerFromLoads` rewrites every load and load-invoice plus grand
invoice (2N+1), then `syncCustomerBalance` writes the customer (1). On a 5-load
order ≈ **19 writes for one payment** against a 20,000/day ceiling.

---

## 3. Phase 2 exit criteria

| # | Criterion | Verified by |
|---|-----------|-------------|
| 1 | Development traffic never hits the production project | Firebase Console Usage flat during a dev session |
| 2 | Concurrent stock writes cannot lose an update | Two-device simultaneous write test (S38) |
| 3 | Stock recording still works with no connectivity | Airplane-mode test (S38) |
| 4 | Sequence numbers are unique and monotonic across create/delete | Delete-then-create test (S37) |
| 5 | Dashboard cold open reads bounded by selected period, not total history | Read counter overlay before/after (S39) |
| 6 | One customer balance sync reads only that customer's documents | Read counter overlay (S40) |
| 7 | Daily reads/writes stay inside free tier under normal factory use | 7-day Console observation |

---

## 4. Architecture decisions (locked for Phase 2)

| # | Decision | Rationale |
|---|----------|-----------|
| A1 | Prefer `FieldValue.increment` over `runTransaction` for stock | `increment` is atomic **and works offline**; `runTransaction` requires a server round trip and would break factory-floor recording |
| A2 | Store `totalQuantity` + `totalValue`; derive `averageCost = totalValue / totalQuantity` | Makes weighted-average cost expressible as two increments, so no read is needed and the result is exact under concurrency |
| A3 | Sequence counters never decrement on delete | Gaps are normal and auditable; duplicates destroy the audit trail |
| A4 | Push the existing `DashboardFinancePeriod` into the Firestore query | Range abstraction already exists; only the query layer changes |
| A5 | No backend migration; stay on Firestore | Repository layer already isolates data access if that changes later |
| A6 | Every sprint ships a measured before/after read count | Prevents "fixed it" without evidence |

---

## 5. Sprint overview

| Sprint | Theme | Fixes | Est. | Risk |
|--------|-------|-------|------|------|
| **S36** | Emulator default + read/write instrumentation | Foundation | 2 days | Low |
| **S37** | Sequence counters | D2 | 3 days | Medium |
| **S38** | Stock atomicity via increments | D1 | 4 days | **High** (data migration) |
| **S39** | Dashboard windowing | D3 | 4 days | Medium |
| **S40** | Ledger scoping + write de-amplification | D4 | 3 days | Medium |
| **S41** | Monthly rollups for "All Time" *(optional)* | D3 durable fix | 3 days | Medium |

**Order matters.** S36 first — it stops dev work from burning quota and gives the
measurement baseline every later sprint is judged against. S38 carries the
migration risk, so it lands after S37 has proven the migration pattern.

---

## 6. Sprint S36 — Emulator default & instrumentation

**Goal:** stop paying for development, and be able to measure reads.

### Tasks

| # | Task | Description | Acceptance criteria |
|---|------|-------------|---------------------|
| 36.1 | Emulator on by default in debug | Invert `FirebaseEmulatorConfig.enabled` so debug builds use emulators unless `--dart-define=USE_PROD_FIREBASE=true` | Debug run connects to emulator with no extra flags; release always uses production |
| 36.2 | Host auto-detection | Keep `10.0.2.2` for Android emulator; document `EMULATOR_HOST` for physical devices | Physical device connects with one dart-define |
| 36.3 | Read/write counter | Debug-only `FirestoreMetrics` wrapper counting document reads, writes, and listener attachments per collection | Counter increments on every repository call |
| 36.4 | On-screen metrics overlay | Debug-only floating overlay showing reads/writes since app start, tappable for per-collection breakdown | Visible in debug, compiled out of release |
| 36.5 | Emulator seed script | Script creating one factory, owner, ~5 customers, ~20 job-work orders, materials, payments | `firebase emulators:start --import` gives a usable dataset |
| 36.6 | Record baseline | Document current reads for: app cold start, dashboard open, create job-work order, record payment | Baseline table committed to this file |

### Deliverables

- `lib/core/config/firebase_emulator_config.dart` (modified)
- `lib/core/observability/firestore_metrics.dart` (new)
- `lib/presentation/widgets/debug/firestore_metrics_overlay.dart` (new)
- `tool/seed_emulator.dart` (new)
- Baseline table in §12

### How to test on device

1. Start emulators on the PC:
   ```
   firebase emulators:start --import=./.emulator-data --export-on-exit
   ```
2. Find your PC's LAN IP (`ipconfig` → IPv4 of your Wi-Fi adapter), then run on a
   physical phone on the same Wi-Fi:
   ```
   flutter run --dart-define=EMULATOR_HOST=192.168.1.10
   ```
3. Confirm the console prints `Firebase emulators: host=... (auth:9099, firestore:8080, functions:5001)`.
4. Open `http://localhost:4000` on the PC — your phone's writes should appear in
   the Emulator UI, **not** in the Firebase Console.
5. **Quota proof:** open Firebase Console → Firestore → Usage, note the read
   count, do 10 minutes of app work on the device, refresh. The count must be
   unchanged.
6. Verify the metrics overlay: cold-start the app, open the dashboard, and record
   the read count for the baseline table.

### Exit

Debug builds cannot touch production. Read counts are observable on-device.

---

## 7. Sprint S37 — Sequence counters

**Goal:** unique, monotonic document numbers at O(1) cost.

### Data model

```
counters/{factoryId}__{sequenceKey}
  factoryId: string
  sequenceKey: string      // 'jobWorkInvoice', 'salesOrder', 'stockIn', ...
  year: number
  value: number            // last allocated
  updatedAt: timestamp
```

### Tasks

| # | Task | Description | Acceptance criteria |
|---|------|-------------|---------------------|
| 37.1 | `SequenceNumberService` | `allocate({factoryId, key, prefix, year})` — `runTransaction` reads counter, writes `value + 1`, returns formatted number | Returns `JWI-2026-0041`; never returns a value twice |
| 37.2 | Seed migration | For each sequence key, scan the collection **once** and set the counter to the current maximum parsed number | Existing documents keep their numbers; next create continues the sequence |
| 37.3 | Migrate 16 call sites | Replace every `_generate*Number` / `generateAgreementNumber` body with a service call | Zero `docs.length + 1` patterns remain in `lib/data/repositories/` |
| 37.4 | Year rollover | On year change, reset `value` to 0 and update `year` inside the same transaction | First document of a new year is `-0001` |
| 37.5 | Offline fallback | Allocation needs connectivity; on failure surface a clear retryable error rather than a timestamp-derived fake (removes the `expense_repository.dart:307` millis fallback) | Offline create shows "number allocation needs connection", no duplicate risk |
| 37.6 | Rules | `counters/{id}` readable/writable only by same-factory authenticated users; `value` may only increase | Rules test: decrement rejected |
| 37.7 | Tests | Unit: format, rollover, monotonicity. Integration: 20 parallel allocations yield 20 distinct numbers | All pass |

### Deliverables

- `lib/data/services/sequence_number_service.dart` (new)
- `lib/data/services/sequence_seed_migration.dart` (new)
- 16 repository files (modified)
- `firestore.rules`, `firestore.indexes.json` (modified)
- `test/data/services/sequence_number_service_test.dart` (new)

### How to test on device

1. **Duplicate-on-delete (the guaranteed bug — verify it first, on the emulator):**
   before the fix, create 3 invoices, delete the last one, create another. It
   reuses the deleted number. After the fix, the new invoice must be `-0004`.
2. **Concurrency, two devices:** install the debug build on two phones signed in
   to the same factory. Open the "create invoice" form on both, fill both, and
   tap Save at the same time (count down out loud). Check the Emulator UI — the
   two invoice numbers must differ.
3. **Single-device race:** with the metrics overlay open, tap Save rapidly 5
   times before the first completes. Expect 5 distinct numbers or clean rejections,
   never a duplicate.
4. **Read cost:** note reads in the overlay before and after creating a job-work
   order. Before ≈ size of 4 collections; after should be ~8 reads regardless of
   history size. Record both in §12.
5. **Seed correctness:** run the migration against the imported emulator dataset,
   then create one document of each type and confirm no number collides with an
   existing one.
6. **Year rollover:** set the device clock to 1 Jan next year, create an invoice,
   confirm `-0001` and that last year's numbers are untouched. Reset the clock.

### Exit

No duplicate document numbers under concurrency or after deletion; create cost is
flat.

---

## 8. Sprint S38 — Stock atomicity

**Goal:** concurrent stock movements cannot lose an update, **without losing
offline recording**.

### Data model change

`rawMaterials/{id}` and `finishedGoods/{id}` gain:

```
totalQuantity: number     // replaces currentStock as source of truth
totalValue:    number     // Σ (qty × unitCost) received
```

`currentStock` / `currentQuantity` and `averageCost` become **derived**:
`averageCost = totalQuantity > 0 ? totalValue / totalQuantity : 0`.
Both legacy fields are kept in sync during a deprecation window (A2, §4).

Stock-in becomes two atomic transforms and **no read**:

```
increment(totalQuantity, +quantity)
increment(totalValue,    +quantity * unitCost)
```

### Tasks

| # | Task | Description | Acceptance criteria |
|---|------|-------------|---------------------|
| 38.1 | Entity + model update | Add `totalQuantity`/`totalValue` to `RawMaterial`, `FinishedGood`; make `averageCost`, `stockValue`, `isLowStock` derived getters | `flutter analyze` clean; existing UI unchanged |
| 38.2 | Backfill migration | For every material/good, set `totalQuantity = currentStock`, `totalValue = currentStock * averageCost` | Post-migration `averageCost` matches pre-migration for every document |
| 38.3 | `recordStockIn` / `recordStockOut` | Replace read-compute-batch with `increment` + transaction doc in one `WriteBatch` | No `getMaterialByType` call before the write |
| 38.4 | Finished goods receipt & adjustment | Same conversion for `finished_goods_repository.dart:191`, `:300` | Per-SKU increments, no pre-read |
| 38.5 | Production batch path | `production_repository.dart:132`, `:273` — material consumption and the delta on update become increments | Batch create touches 5 collections with no stale reads |
| 38.6 | Negative-stock guard | Client pre-check kept for UX; authoritative guard added in `firestore.rules` comparing incoming vs existing | Rules test: write driving `totalQuantity` below 0 rejected |
| 38.7 | Reconciliation report | Debug screen comparing `totalQuantity` against the sum of `stockTransactions` per material | Any drift is visible and attributable |
| 38.8 | Tests | Unit: derived average cost, negative guard. Integration: 10 parallel stock-ins land exactly | All pass |

### Deliverables

- `lib/domain/entities/raw_material.dart`, `finished_good.dart` (modified)
- `lib/data/models/raw_material_model.dart`, `finished_good_model.dart` (modified)
- `raw_material_repository.dart`, `finished_goods_repository.dart`, `production_repository.dart` (modified)
- `lib/data/services/stock_backfill_migration.dart` (new)
- `lib/presentation/screens/debug/stock_reconciliation_screen.dart` (new)
- `firestore.rules` (modified)
- `test/data/repositories/stock_atomicity_test.dart` (new)

### How to test on device

1. **Reproduce the bug first, on the emulator.** Two phones, same material.
   Phone A records stock-in of 50 tons, Phone B records 30 tons — tap Save
   simultaneously. Before the fix: final stock is 130 or 150, not 180, while
   **both** `stockTransactions` rows exist. Screenshot this; it is the proof the
   fix is needed.
2. **Verify the fix.** Repeat step 1 after the change. Final `totalQuantity` must
   be exactly 180, and `averageCost` must equal
   `(50×costA + 30×costB) / 80`. Check in the Emulator UI.
3. **Offline recording (this is the point of A1).** Put the phone in airplane
   mode, record a stock-in, confirm the UI updates immediately from the local
   cache. Re-enable network and confirm the value lands on the server. This must
   pass — if it fails, the increment approach was not applied correctly.
4. **Offline concurrency.** Phone A offline records +50; Phone B online records
   +30; bring A back online. Final must be +80, not +50 or +30. This is the case
   `runTransaction` cannot handle and is the main reason for A1.
5. **Negative stock.** Try to record stock-out larger than available. Expect a
   clear client error; then verify via a direct Emulator UI write attempt that the
   rule also rejects it server-side.
6. **Migration safety.** Export the emulator dataset, run the backfill, and use
   the reconciliation screen to confirm every material's `averageCost` and
   `stockValue` are unchanged. **Do not run against production until this passes.**
7. **Production batch.** Create a batch consuming raw material and producing
   graded slabs; confirm raw stock down, finished goods up, and both transaction
   rows written, with the P&L material cost unchanged.

### Exit

Stock is atomic, offline recording still works, and existing valuations are
provably unchanged by the migration.

---

## 9. Sprint S39 — Dashboard windowing

**Goal:** dashboard read cost proportional to the selected period, not to total
history. This is the sprint that fixes the quota.

### Tasks

| # | Task | Description | Acceptance criteria |
|---|------|-------------|---------------------|
| 39.1 | Range reaches the query | Add `{DateTime? from, DateTime? to}` to the `watch*` methods the dashboard uses; apply `where(dateField, >=, from)` | Query-level filter replaces `contains(date, start, end)` post-filtering |
| 39.2 | Period-driven resubscribe | `DashboardWatchStarted` carries `DashboardFinancePeriod`; changing the period re-subscribes with the new window instead of re-filtering in memory | Switching Daily→Monthly changes the query, not just the maths |
| 39.3 | Live vs snapshot split | Keep `snapshots()` only for payments, invoices, job-work orders, sales orders, customers. Convert equipment, QC, production batches, attendance, employees to one-shot `.get()` with manual refresh | Listener count drops from 17 to ≤6 |
| 39.4 | Bound every list | Add `limit()` with an explicit cap to remaining dashboard queries | No unbounded dashboard query remains |
| 39.5 | "All Time" handling | Cap at a rolling 24 months (or serve from S41 rollups when available), with the UI labelling it accurately | All Time no longer scans unbounded history |
| 39.6 | Debounce re-attach | Guard `_onWatchStarted` so navigating back to the dashboard reuses live subscriptions instead of cancel-and-reattach | Re-entering the dashboard costs ~0 additional reads |
| 39.7 | Indexes | Add `factoryId + <dateField>` composite indexes for every windowed collection | No `failed-precondition` index errors in any period |
| 39.8 | Tests | Bloc tests: period change triggers new subscription; widget tests still pass | All pass |

### Deliverables

- `lib/blocs/dashboard/dashboard_bloc.dart`, `dashboard_event.dart` (modified)
- ~12 repository files: windowed `watch*`/`fetch*` overloads
- `firestore.indexes.json` (modified)
- `test/blocs/dashboard/dashboard_windowing_test.dart` (new)

### How to test on device

1. **Baseline.** With the metrics overlay open, cold-start the app and open the
   dashboard on the seeded dataset. Record total reads and the per-collection
   breakdown. Note it should roughly equal total document count.
2. **After the change,** repeat with period = Daily. Reads should be a small
   fraction of the baseline. Record both numbers in §12.
3. **Period switching.** Cycle Daily → Weekly → Monthly → Yearly and watch the
   overlay. Each switch should add reads proportional to the window; Daily must be
   far cheaper than Yearly. If they cost the same, the filter is still client-side.
4. **Re-entry cost (36.6 regression).** Navigate Dashboard → Job Work → Dashboard
   five times. Additional reads should be near zero. Before the fix this was 5×
   full re-read.
5. **Correctness, not just cheapness.** For one period, compare every dashboard
   KPI against the pre-change build on the same dataset — revenue, receivables,
   dispatch, stock-cut. Numbers must match exactly. A cheaper but wrong dashboard
   is a worse outcome than an expensive one.
6. **Index errors.** Exercise all six periods and watch logcat
   (`flutter logs`) for `failed-precondition`. Deploy indexes with
   `firebase deploy --only firestore:indexes` and re-test.
7. **Real-device quota check.** Point a debug build at a **staging** Firebase
   project, use the app normally for an hour, and compare Console reads against
   the same exercise on the old build.

### Exit

Dashboard cold open and re-entry are bounded and measured; all KPIs unchanged.

---

## 10. Sprint S40 — Ledger scoping & write de-amplification

**Goal:** stop reading the whole factory to sync one customer, and stop writing
the same documents three times per payment.

### Tasks

| # | Task | Description | Acceptance criteria |
|---|------|-------------|---------------------|
| 40.1 | Scope sales orders | Replace `watchSalesOrders(factoryId).first` with a `customerId`-filtered one-shot `.get()` | Reads only that customer's orders |
| 40.2 | Scope job-work loads | Replace `watchLoads(factoryId).first` with a load query filtered by the customer's job-work IDs | Reads only relevant loads |
| 40.3 | Remove `.first`-on-stream | Audit every `.first` on a `snapshots()` stream and convert to `.get()` | No `snapshots().first` remains in `lib/` |
| 40.4 | De-duplicate payment sync | `recordJobWorkPayment` already updates invoice+load+order atomically; make the follow-up `_syncInvoiceFromPayments` (`payment_repository.dart:894`) conditional so it runs only on repair paths, not the happy path | One payment writes once per document; recomputation still available for repair |
| 40.5 | Bound container refresh | `refreshContainerFromLoads` should write only documents whose values actually changed | No-op payment causes 0 writes |
| 40.6 | Indexes | `salesOrders`, `jobWorkLoads` composite indexes on `factoryId + customerId` | No index errors |
| 40.7 | Tests | `customer_balance_calculator_test.dart` extended; balance after payment identical to pre-change | All pass |

### Deliverables

- `lib/data/services/customer_ledger_service.dart` (modified)
- `payment_repository.dart`, `job_work_load_repository.dart` (modified)
- `firestore.indexes.json` (modified)

### How to test on device

1. **Read cost of one payment.** Reset the overlay, record a job-work payment,
   read the counter. Before: hundreds of reads (all factory sales orders + all
   loads). After: tens. Record both in §12.
2. **Write cost of one payment.** Same procedure for the write counter. On a
   5-load order, before ≈19 writes; target ≤8. Verify in the Emulator UI that
   each document has one new version, not three.
3. **Balance correctness — the thing that must not regress.** Pick a customer
   with mixed history: advance payment, partial payment, multiple loads, a
   cancelled order. Record `balance`, `totalAmountPaid`, `totalBalanceDue` before
   the change. After the change, re-run the sync and confirm all three are
   identical. Repeat for a customer in credit (negative balance).
4. **Statement export.** Export that customer's PDF statement before and after;
   opening balance, debits, credits, and closing balance must match line for line.
5. **Idempotency.** Trigger the sync three times in a row for the same customer.
   The balance must not drift, and runs 2 and 3 should write nothing.
6. **Multi-device.** Phone A records a payment while Phone B has the customer
   detail screen open. B should reflect the new balance, and the totals must stay
   correct.

### Exit

Single-customer sync reads only that customer's data; one payment writes each
document once.

---

## 11. Sprint S41 — Monthly rollups *(optional)*

**Goal:** the durable fix for "All Time" and for growth beyond a couple of years.

Only needed if, after S39, All Time is still expensive or the factory's history
grows faster than expected. Defer until measured.

### Data model

```
dashboardRollups/{factoryId}__{yyyy-MM}
  revenueSales, revenueJobWork, expenses, collected,
  outstanding, stockCutSqFt, dispatchedSqFt, ...
```

### Tasks

| # | Task | Description | Acceptance criteria |
|---|------|-------------|---------------------|
| 41.1 | Rollup entity + model | Monthly aggregate per factory | Serialises cleanly |
| 41.2 | Update on write | Apply deltas via `FieldValue.increment` when payments/expenses/loads change | Rollup matches recomputation |
| 41.3 | Backfill | One-off job building rollups from existing history | Backfilled totals match the current dashboard |
| 41.4 | Dashboard source switch | Periods ≥ 6 months read rollups (~24 docs) instead of raw documents | All Time costs ~24 reads |
| 41.5 | Drift detector | Debug action recomputing one month from raw data and comparing | Drift surfaced, not silent |

### How to test on device

1. Backfill on the emulator dataset, then compare every All Time KPI against the
   S39 build. Must match exactly.
2. Record a payment and confirm the current month's rollup increments without a
   full recomputation.
3. Confirm All Time read count is flat (~24) whether the dataset has 1,000 or
   50,000 documents — seed both and compare.
4. Run the drift detector after a day of mixed activity; expect zero drift.

---

## 12. Measurement log

Filled in as sprints land. **Every row must be measured on-device with the S36
overlay, on the same seeded dataset.**

| Operation | Baseline (pre-S36) | After S37 | After S39 | After S40 | Target |
|-----------|--------------------|-----------|-----------|-----------|--------|
| App cold start | — | — | — | — | < 50 reads |
| Dashboard open (Daily) | — | — | — | — | < 200 reads |
| Dashboard re-entry | — | — | — | — | ~0 reads |
| Create job-work order | — | — | — | — | < 15 reads |
| Record payment (reads) | — | — | — | — | < 40 reads |
| Record payment (writes) | — | — | — | — | ≤ 8 writes |
| Record stock-in | — | — | — | — | ≤ 3 writes, 0 reads |

---

## 13. Migration checklist (existing deployments)

Run in this order. **S37 and S38 both migrate live data — back up first.**

1. Export production Firestore:
   `gcloud firestore export gs://<bucket>/pre-phase2`
2. Deploy rules + indexes: `firebase deploy --only firestore:rules,firestore:indexes`
3. Run the S37 sequence seed migration; verify counters ≥ current max per key.
4. Release the S37 build; confirm new document numbers continue the sequence.
5. Run the S38 stock backfill; verify with the reconciliation screen that
   `averageCost` and `stockValue` are unchanged for every material.
6. Release the S38 build; watch for negative-stock rejections in logs.
7. Release S39 and S40 (no data migration).
8. Observe Console usage for 7 days against the free-tier ceiling.

---

## 14. Risks & mitigations

| Risk | Impact | Mitigation |
|------|--------|------------|
| S38 backfill corrupts valuations | **Critical** | Export first; reconciliation screen; emulator dry run on imported production copy |
| Derived `averageCost` changes historical reports | High | Keep legacy fields written during deprecation window; compare P&L before/after |
| Sequence seed set too low → duplicate numbers | High | Seed from parsed maximum, not count; verify per key before release |
| Dashboard windowing changes KPI values | High | Task 39.5 requires exact KPI match against the old build before merge |
| Removing `_syncInvoiceFromPayments` hides a real repair path | Medium | Make it conditional, not deleted; keep a manual "recompute" action |
| Missing composite index crashes a period | Medium | Exercise all six periods in QA; deploy indexes before app release |
| Offline behaviour regresses | High | Airplane-mode test is a release gate for S38 |

---

## 15. Definition of done (per sprint)

1. `flutter analyze` clean; `flutter test` green.
2. New unit/integration tests for the fixed behaviour.
3. The bug reproduced **before** the fix and shown fixed **after**, on device.
4. Before/after read and write counts recorded in §12.
5. Offline path verified where the sprint touches writes.
6. Rules and indexes deployed and exercised.
7. No feature behaviour change: KPIs, balances, and statements byte-identical.
